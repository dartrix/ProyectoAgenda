<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dartrix.proyectoagenda">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity"></activity>
        <activity android:name=".AgregarMateriaActivity"></activity>
        <activity android:name=".MostrarMateriaActivity"></activity>
        <activity android:name=".AgregarAsignacionActivity"></activity>
        <activity android:name=".MostrarAsignacionActivity"></activity>
        <activity android:name=".EditarMateriaActivity"></activity>

        <activity android:name=".asignacionesActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
            </activity>
        <activity android:name=".Notificacion"/>
        <activity android:name=".Repeating_activity"/>
        <activity android:name=".EditarAsignaturaActivity"/>
        <receiver android:name=".Notification_reciever"/>
    </application>

</manifest>